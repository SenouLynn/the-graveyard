import { Meta } from '@storybook/blocks';


<Meta title="Templates/Templates - Guide" />

# Templates

Templates are page-level objects that place components into a layout and articulate the design’s underlying content structure.

Generally these should be application level builds however it could be useful to build base templates in the UI lib then copy the logic into a data driven environment.