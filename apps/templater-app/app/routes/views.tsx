/**
 *
 * Views:
 *  - Hardcode all first n data shapes as we enter them into the system
 *  - Generally the materials will be:
 *      - Base type (ex: User)
 *      - Create [type] (ex: api.createUser)
 *      - Get [type] for id (ex: api.getUser)
 *      - Get all [type]s: (ex: api.getAllUsers)
 *      - Get some [type] (ex: api.getUsersForFilter)
 *      - Update part of [type] (ex: api.updateUser)
 *      - Delete [type] (ex: api.deleteUser)
 *
 * Views should deal with the display of data
 *
 */
export default function Views() {
  return <div>Views</div>;
}
